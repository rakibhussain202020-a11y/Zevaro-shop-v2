import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {createSupabaseServerClient} from '@/lib/supabase/server';
async function admin(){const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return false;const {data}=await s.from('admins').select('id').eq('id',user.id).maybeSingle();return !!data}
function db(){return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{autoRefreshToken:false,persistSession:false}})}
export async function GET(){if(!await admin())return NextResponse.json({error:'Forbidden'},{status:403});const {data,error}=await db().from('orders').select('id,customer_name,mobile,email,total,status,payment_method,created_at,courier,tracking_number').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({orders:data||[]})}
export async function PATCH(req:Request){if(!await admin())return NextResponse.json({error:'Forbidden'},{status:403});const {id,status,courier,tracking_number}=await req.json();const {error}=await db().from('orders').update({status,courier,tracking_number}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
