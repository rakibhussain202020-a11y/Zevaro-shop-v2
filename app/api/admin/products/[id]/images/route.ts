import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {createSupabaseServerClient} from '@/lib/supabase/server';
async function ok(){const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return false;const {data:a}=await s.from('admins').select('id').eq('id',user.id).maybeSingle();return !!a}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){if(!await ok())return NextResponse.json({error:'Forbidden'},{status:403});if(!process.env.SUPABASE_SERVICE_ROLE_KEY)return NextResponse.json({error:'Server storage key missing'},{status:503});const {id}=await params;const form=await req.formData();const file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'Image file required'},{status:400});if(!file.type.startsWith('image/')||file.size>8*1024*1024)return NextResponse.json({error:'Image must be under 8MB'},{status:400});const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{autoRefreshToken:false,persistSession:false}});const path=`${id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const {error:upload}=await db.storage.from('product-images').upload(path,await file.arrayBuffer(),{contentType:file.type,upsert:false});if(upload)return NextResponse.json({error:upload.message},{status:400});const {data:existing}=await db.from('product_images').select('sort_order').eq('product_id',id).order('sort_order',{ascending:false}).limit(1);const sort=(existing?.[0]?.sort_order||0)+1;const {error}=await db.from('product_images').insert({product_id:id,storage_path:path,sort_order:sort});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true,path})}
