'use client';
import { useState } from 'react';
import { createBrowserClient } from '@supabase/ssr';
import { useRouter } from 'next/navigation';
export default function Register(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [ok,setOk]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const r=await s.auth.signUp({email,password,options:{data:{full_name:name}}});if(r.error){setError(r.error.message);return}if(r.data.session)router.push('/account');else setOk(true)}return <main className="container-z py-16 max-w-md"><h1 className="serif text-4xl">Create account</h1>{ok?<p className="mt-6 text-sm text-muted">Check your email to confirm your account, then log in.</p>:<form onSubmit={submit} className="space-y-3 mt-8"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full Name" className="w-full border hairline px-4 py-3"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full border hairline px-4 py-3"/><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)" className="w-full border hairline px-4 py-3"/>{error&&<p className="text-red-600 text-sm">{error}</p>}<button className="w-full bg-black text-white py-4 text-xs tracking-[.15em]">CREATE ACCOUNT</button></form>}</main>}
