'use client';
import {useEffect,useState} from 'react';
export default function OrdersAdmin(){const [orders,setOrders]=useState<any[]>([]);async function load(){const r=await fetch('/api/admin/orders');if(r.ok)setOrders((await r.json()).orders||[])}useEffect(()=>{load()},[]);async function status(id:string,status:string){await fetch('/api/admin/orders',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,status})});load()}return <main className="container-z py-10"><div className="flex justify-between"><h1 className="serif text-4xl">Orders</h1><a href="/admin" className="underline text-sm">Dashboard</a></div><div className="overflow-x-auto mt-8"><table className="w-full text-sm"><thead><tr className="border-b hairline text-left"><th className="py-3">Order</th><th>Customer</th><th>Amount</th><th>Status</th><th>Change</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-b hairline"><td className="py-3"><b>{o.id}</b><small className="block text-muted">{o.mobile}</small></td><td>{o.customer_name}</td><td>₹{Number(o.total).toLocaleString('en-IN')}</td><td>{o.status}</td><td><select value={o.status} onChange={e=>status(o.id,e.target.value)} className="border px-2 py-2"><option>Order Placed</option><option>Confirmed</option><option>Processing</option><option>Packed</option><option>Shipped</option><option>Out for Delivery</option><option>Delivered</option><option>Cancelled</option><option>Return Requested</option><option>Returned</option><option>Refunded</option></select></td></tr>)}</tbody></table></div></main>}
