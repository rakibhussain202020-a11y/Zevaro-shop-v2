'use client';import {useState} from 'react';export function NewsletterForm(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('');const r=await fetch('/api/newsletter',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMsg(r.ok?'Thank you.':d.error||'Could not subscribe.');if(r.ok)setEmail('')}return <div><form onSubmit={submit} className="max-w-md mx-auto flex mt-6"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="flex-1 border hairline px-4 py-3 text-sm outline-none"/><button className="bg-black text-white px-5 text-xs tracking-[.12em]">SUBSCRIBE</button></form>{msg&&<p className="text-xs text-muted mt-3">{msg}</p>}</div>}
